pipeline {
    agent any
    stages {
        stage('Build') {
            steps { echo 'Building dummy code...' }
        }
        stage('Test') {
            steps { echo 'Running tests...' }
        }
        stage('Deploy') {
            steps { echo 'Deploying to Nginx...' }
        }
    }
}
